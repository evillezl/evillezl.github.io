#built-inmethod: code available in python 
    #example print () or range ()

#other built-in methods
    #round (#,2 (placed value it will round)) How much it will round in to?
    #min, max looks at the values in what ever placed they are and see which it is higher or lower value
    #math.ceil(x) returns to the next smallest
    #math.floor(x) returns to the previous smallest
    #math.pow(x, y) it is x to the power of y
    #math.sqrt (x) returns to the square root absol
    #math.isqrt (x) returns to the closest square root 
    #math.abs (x) returns to the positive value
    #math.fabs(x) returns to the absolute value
    #math.max(x,y) returns to the largest value
    #math.floor(x, y) returns to the smallest value
    #math.sin (x)
    #math.cos (x)
    #math.tan (x)
     #it will return to either caculation

#example of math usage
import math
num = (9)
print (abs(num))
print (math.sqrt(num))
print (math.sin(num))

    #always put import math

#Random numbers
    #generate algorithm to generate "random" numbers in squence

#example of generating pseudo-random numbers
import random

num = random.randrange(1,20)
print (num)
